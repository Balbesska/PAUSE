/*const bcrypt = require('bcrypt');

// Предположим, что у вас есть переменная password, содержащая введенный пароль
const password = 'kulakova';

// Хеширование пароля
bcrypt.hash(password, 10, (err, hash) => {
    if (err) {
        console.error('Ошибка при хешировании пароля:', err);
    } else {
        // Теперь переменная hash содержит хеш пароля, который можно сохранить в базу данных
        console.log('Хешированный пароль:', hash);
    }
});
*/